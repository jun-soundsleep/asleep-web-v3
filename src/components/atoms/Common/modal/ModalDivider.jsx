import React from 'react';
import styled from '@emotion/styled';

function ModalDivider(props) {
  return <Container />;
}

export default ModalDivider;

const Container = styled.div`
  height: 1px;
  background-color: var(--components-components-05);
`;
